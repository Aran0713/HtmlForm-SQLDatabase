CREATE TABLE `datatest`.`Person` (
    `firstname` varchar(100) NOT NULL,
    `lastname` varchar(100) NOT NULL,
    `age` int NOT NULL,
    `id` varchar(100) NOT NULL,
	PRIMARY KEY (`id`));

SELECT * FROM datatest.person;